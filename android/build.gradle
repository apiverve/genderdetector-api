plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.12'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'genderdetector-api'
            version = '1.1.12'

            from components.java

            pom {
                name = 'Gender Detector'
                description = 'Easily detect the gender of an individual based on their name. This API uses AI to predict the likely gender of a person by their name'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/genderdetector-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/genderdetector-api.git'
                    url = 'https://github.com/apiverve/genderdetector-api'
                }
            }
        }
    }
}
